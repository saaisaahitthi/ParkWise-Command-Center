# Python 3.13-compatible pins (minimum bumps from requirements.txt).
# Root cause: pandas==2.2.2 and numpy==1.26.4 have no cp313 wheels on Windows,
# so pip falls back to a source build that fails (Meson / vswhere).
#
# Install:
#   pip install -r requirements-py313.txt
#
# Prefer binary wheels only (recommended on Windows):
#   pip install --only-binary=pandas,numpy,scipy,scikit-learn -r requirements-py313.txt

# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
geoalchemy2==0.14.7
psycopg2-binary==2.9.10
alembic==1.13.1

# ─── Configuration & Environment ──────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ─── Data Processing (Python 3.13 wheel support) ───────────────────────────────
pandas==2.2.3
numpy==2.1.3
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.0

# ─── Machine Learning (Python 3.13 wheel support) ───────────────────────────
scikit-learn==1.5.2
lightgbm==4.5.0
shap==0.52.0
scipy==1.14.1

# ─── Geospatial & Routing ─────────────────────────────────────────────────────
osmnx==1.9.3
networkx==3.3

# ─── Async & HTTP ─────────────────────────────────────────────────────────────
httpx==0.27.0
anyio==4.3.0

# ─── Logging & Monitoring ─────────────────────────────────────────────────────
structlog==24.1.0
python-json-logger==2.0.7

# ─── Utilities ────────────────────────────────────────────────────────────────
orjson==3.10.15
python-dateutil==2.9.0
pytz==2024.1
joblib==1.4.2

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
